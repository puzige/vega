# Issue #73 — MCP v1 test-first delivery matrix

Status: **implementation and final candidate tested; matrix acceptance remains partial on remaining native/fault cases** · 2026-09-20

Contract: [vega-issue-73-mcp.md](vega-issue-73-mcp.md) · Issue: https://github.com/puzige/vega/issues/73

This table preserves the frozen requirements and now records an honest post-implementation audit. `PASS` means the whole row's stated slice was exercised; `PARTIAL` means a green unit/headless test must not be substituted for the missing native/fault proof. The first final candidate's private screenshot hashes, real-model local/OAuth calls, DB audit and daily-app rollback are recorded outside the repository in `vega-evidence/issue-73-2026-09-20/native-final.yzuipo/native-final-manifest.md`. The later #74-based native same-name routing check, exact screenshot hashes, independent server traces, persistence, and another exact daily-app rollback are in `vega-evidence/issue-74-2026-09-20/native-provenance.mMvVQJ/native-provenance-manifest.md`. No user credential is included here. A CIMD-only service still needs a public HTTPS metadata identity; pre-registered and DCR-capable services do not wait for it.

## Test matrix

Each deterministic fixture uses owned temporary configuration/data and harmless local HTTP/stdio servers. “Owned HTTP” means a real loopback server handling the actual production client requests; it is not a stubbed return value from the MCP client. MockProvider is permitted for M02–M14 only at the model boundary and must not be described as a real model or complete user acceptance. M15 uses the user's existing configured real model (without capturing its credential) and actual MCP servers; owned loopback servers are acceptable. Current evidence and residuals are itemized below.

| ID | Requirement / risk | Pre-state | Operation | Observable result | Layer | Evidence to retain | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| M01 | UI-only durable configuration and activation | Clean owned config, no MCP child | Add local and remote entries through Settings; save, restart, enable then disable | Both identities/types persist; disabled creates no process/network call or advertised tool; no config/DB editing in acceptance | GPUI production handler + native app | Handler event trace, restarted screen and config redaction check | PARTIAL — native UI save/enable/disable/restart and inert disabled local passed; consolidated redaction evidence still open |
| M02 | Modern stdio protocol | Enabled owned 2026-07-28 server | Test connection, list, model calls a text tool | Real child receives `server/discover` and per-request `_meta`; IDs match; one audited result reaches next model round | E2E-REAL | Server request capture with no secrets, DB audit and provider request | PASS — `m02_modern_stdio_real_child_round_trip`, owned stdio conversation second-round test, final native call |
| M03 | Legacy stdio downgrade safety | Two owned servers: 2025-11-25 and modern-error | Connect both | Legacy uses `initialize`; recognized modern version error negotiates/fails without legacy downgrade | E2E-REAL + FAULT-INJECTION | Per-server wire transcript and status | PASS — `m03_legacy_stdio_only_after_nonmodern_probe`, `m03_modern_version_error_never_initializes_legacy` |
| M04 | Modern remote JSON/SSE transport | Owned Streamable HTTP endpoint | Connect; list via JSON; call once via JSON and once via request-scoped SSE | Each is POST with matching version/method/name/body, correct Accept, ID and result; no session/GET stream | E2E-REAL | Sanitized HTTP request/response transcript | PASS for headless owned endpoint — `m04_modern_http_json_and_request_scoped_sse` and owned HTTP conversation test |
| M05 | Legacy HTTP compatibility boundary | Owned 2025-11-25 Streamable HTTP and standalone HTTP+SSE endpoints | Connect both from UI | Documented era fallback works for the former; latter reports unsupported without silently using old transport | E2E-REAL | HTTP transcript and native error state | PARTIAL — transport tests pass; both native UI error states not captured |
| M06 | OAuth metadata, DCR/redirect/consent and token isolation | Owned OAuth resource/AS fixtures: pre-registered, advertised DCR, rejected DCR and hostile redirect/issuer | Trigger 401; select pre-registration or confirm DCR in UI; exercise DCR success/failure, callback, call, refresh, step-up and disconnect | PKCE S256/state/issuer/resource/scopes valid; native registration and issuer binding; failed registration/redirect makes zero token request to wrong origin; local tokens cleared with grant caveat | E2E-REAL + native UI | Sanitized auth/DCR request capture, redaction/file-permission check, UI screenshots | PARTIAL — pre-registered native OAuth/call and fault tests pass; native DCR/disconnect/step-up evidence open |
| M07 | Anonymous/manual bearer secrecy | Two owned endpoints and an owner-only test secret | Connect anonymous and bearer; exercise redirect and changed origin | Anonymous has no Authorization; bearer only exact URL/origin; no key in config, DB, logs, card or redirected request | E2E-REAL + UNIT | Sanitized request capture and absence checks | PARTIAL — exact-origin/redirect/SQLite mask tests pass; final absence sweep not retained |
| M08 | Catalog pagination and collision | Two servers expose same `search`; one emits bad schema/header annotation and cursor cycle | Refresh tools and start task | All valid pages consumed within cap; aliases stable/distinct; bad/cyclic entries rejected visibly; no built-in/Skill name hijack | E2E-REAL + UNIT | Frozen catalog and rejection codes | PARTIAL — pagination/schema/cycle/alias and production-entry provenance tests pass; two same-name stdio servers were selected correctly in opposite real-model tasks with one independently traced call each and distinct audited aliases; native bad/cyclic rejection and built-in/Skill collision proof remain open |
| M09 | Capability/permission fail-closed | Ask, Plan, Execute ReadOnly/Confirm/Auto/FullAccess tasks | Propose MCP call; deny, timeout, cancel and approve where eligible | Ask/Plan/ReadOnly zero dispatch; all eligible modes prompt per call; denied/timeout/cancel zero dispatch; approved exactly once | E2E-REAL + GPUI test | Permission events, child/HTTP call counters, DB audit | PARTIAL — mode/approval/denial/timeout tests and native one-shot approval pass; full real-server mode matrix open |
| M10 | Deterministic model→tool→model lifecycle, not final real-model acceptance | Ready owned MCP server, Execute task and MockProvider | MockProvider selects an MCP tool; user approves; provider emits follow-up answer | One true MCP server call, one durable card, bounded result in next provider round, truthful status after reopen | E2E-REAL at MCP boundary + native UI | MockProvider request, server count, screenshot, audit row | PARTIAL — owned stdio/MockProvider and native durable cards pass; final combined trace open |
| M11 | Hostile/malformed/large server data | Owned server emits errors, oversized line/event/catalog/schema/arguments, stalled SSE, unsupported content | Connect/call each case | Protocol vs tool error distinguished; every count/byte/time ceiling in spec §3.4 enforced; no partial success, coercion or unbounded buffering | FAULT-INJECTION | Failure code, bounded memory/byte counters | PASS — protocol/wire fault tests cover the 256 KiB/1 MiB/4 MiB/8 MiB and 15s/30s/120s ceilings |
| M12 | Cancel/disconnect/crash safety | Approved running local and HTTP calls, persisted running row | Stop, kill child, drop HTTP, restart app | stdio cancel or child reaped; HTTP response stream closed; uncertain side effect never auto-replayed; new explicit call uses new ID | E2E-REAL + restart | Process/HTTP counters and pre/post DB state | PARTIAL — child/HTTP cancellation and store recovery tests pass; native mid-call crash/restart evidence open |
| M13 | Run-start freeze vs revocation | Running task with frozen catalog | Refresh tool list, change auth, disable server | No new tool alias/schema appears in active run; disable/revoke blocks subsequent dispatch and cancels active call; next run sees current state | E2E-REAL | Two provider schemas, dispatch counters, UI state | PARTIAL — freeze/revoke/replace tests pass; final two-round provider-schema trace open |
| M14 | Shared #74 registry and #76 budget | MCP schemas plus reserved `load_skill`/`read_skill_resource`, near context trigger | Start run, use tool, compact with hostile returned text | Names collision-free, all schemas estimated each round, frozen Skill system envelope owned by #74, external text untrusted, no permission/prompt escalation | E2E-REAL + UNIT | Frozen schema, budget estimate and authority assertions | PARTIAL — #74 combined test proves distinct stable aliases for two owned stdio `echo` children, reserved Skill schemas, per-round budget/compaction, hostile-result authority rejection and independent MCP denial; final native real-model run loaded/read a Skill then called one approved local `echo`. Native same-name model routing, near-limit/hostile real-model loop, and remote OAuth combination remain open |
| M15 | Native end-to-end release proof with a real model | Installed build, existing configured real model, and actual owned local/OAuth-protected remote MCP servers | Configure both servers and remote OAuth in UI; ask the real model to use each discovered tool; approve harmless calls, receive answers, restart | Actual local and authenticated HTTP servers each receive a call selected by the real model; genuine answers and audited cards persist; light/dark/narrow and keyboard/focus paths usable; no manual config edit. MockProvider cannot pass | Native E2E + real-model smoke | Persistent screenshots with build ID, hashes and manifest; sanitized model tool-call and server request traces, never model credentials | PARTIAL — real local/OAuth calls, one-shot approvals, restart and light/dark/960×600 evidence captured; keyboard/focus and combined sanitized trace open |

## Proposed implementation gates and handoff

M06 requires distinct negative fixtures for DCR 4xx/invalid response, callback `state` mismatch, missing/mismatched `iss`, unapproved redirect or issuer change, expired token/refresh failure, and cross-origin credential leakage. The happy fixture must exercise registration only after the UI confirmation and must prove a genuine `tools/call` with the resulting access token. None uses an external account.

1. Freeze spec decisions before code. Check whether the existing approved HTTP/JSON/SSE/hash/base64 crates and OS CSPRNG cover implementation. If any new crate is truly required, stop under `vega-exec-guide.md` §3/§5/§6 and give the architect its exact pinned version, license, transitive dependency inventory and reason the approved stack is insufficient; obtain approval **before** editing `Cargo.toml`/`Cargo.lock`. Main agent delegates implementation to a dedicated subagent in a fresh sibling worktree. Keep #76 correction integration and shared tool-registry editing serial; do not let #73 and #74 edit the same loop/permission/persistence files concurrently.
2. Implement transport, headless OAuth and owned servers first; demonstrate the absent transport failing then focused protocol/auth tests passing through the production client. Implement the shared registry, credential persistence, permission/audit and app UI in one integration owner; prove M09–M14. Do not modify existing safety-test assertions merely to pass.
3. Under the repository-wide Cargo lock, run focused packages, `fmt --all -- --check`, strict Clippy, `test --workspace`, build/package and dependency-direction check. Capture exact commands, exit codes, pass/fail/ignored counts and raw log paths. No concurrent build/test across shared target worktrees.
4. Run M15 on the installed artifact with an existing configured real model, an owned local server, and an owned OAuth-protected remote server configured/authenticated through Settings. The real model must propose each tool call; owned servers must receive them. If no real model is configured, M15 remains blocked, not passed by M10. An external third-party MCP service may add interoperability evidence but cannot replace owned deterministic HTTP/OAuth E2E or be the only proof. Keep private captures and credentials out of the public Issue; retain screenshot hashes and a manifest outside worktrees.
5. Review against the matrix, integrate only after #76 shared-seam stabilization, rerun affected gates after integration, then follow the repo's merge, cleanup, Issue write-back and Project state protocol. A green headless test is evidence for only its slice, never an automatic pass for the full row.

## Delivery evidence snapshot

The table below records the earlier OAuth/local native package; its code commit and log paths are historical, not the current PR HEAD. The later same-name routing package and rollback are in `vega-evidence/issue-74-2026-09-20/native-provenance.mMvVQJ/native-provenance-manifest.md`. After the test-only SSE timer repair, `./scripts/cargo-lock.sh test --workspace --locked`, strict Clippy, format check, and locked workspace build all exited 0 on the #73 worktree. The subsequent #74 rebase, still with the same production binary, passed 1,626 tests across 35 groups with 0 failures and 9 ignored.

| Item | Value |
| --- | --- |
| Verified UTC/local time | 2026-09-20; final native interaction recorded in the private manifest |
| Earlier branch / build commit / tracked diff hash | `feat/issue73-mcp-integration` / code commit `ea7ef887` / pre-documentation tree `29f39755`; installed binary SHA-256 `d0d1ca6ad7708a7803e90db07d1437a9d367a9a6fcad92ed2f12766be7d7c168` |
| Owned server and OAuth fixture identities | In-repo `owned_stdio_server` and loopback `owned_oauth_server`; pre-registered `owned-client`; no user secret retained |
| Format / Clippy / tests / build raw logs | Private `vega-evidence/issue-73-2026-09-20/final-{fmt-r2,clippy-r2,workspace-test-r5,package-r2}.log`; 1541 passed, 0 failed, 9 ignored across 35 suites |
| Native screenshots / SHA-256 / evidence manifest | Private `vega-evidence/issue-73-2026-09-20/native-final.yzuipo/native-final-manifest.md`, with individual screenshot hashes and installed binary hash |
| Accepted residuals, rollback and integration commit | M01/M05–M10/M12–M15 partial; M14 has #74 headless and native local combined evidence but same-name/remote/fault cases remain; original app, data and config restored; #73 draft PR and #74 integration remain pending |
