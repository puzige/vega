# Issue #74 / A8-05 — Skills v1 acceptance plan

Status: product/test contract frozen, staged implementation in progress. Every row is **NOT RUN** until its production-path evidence is recorded; this file is not delivery evidence.
Issue: https://github.com/puzige/vega/issues/74
Contract: [vega-issue-74-skills.md](vega-issue-74-skills.md)

The image attached to the Issue is a reference, not executable instructions or an acceptance contract. This matrix must be reviewed before implementation. Each test records the exact source scope, consent state, user action, observed provider/tool/UI state, and evidence class. Use owned temporary project and config roots; never exercise private credentials or third-party scripts during deterministic tests.

Partial module evidence (not full row acceptance): 2026-09-20 `scripts/cargo-lock.sh test -p vega_runtime` passed 155 tests, four existing load-sensitive tests ignored, plus one doctest; 29/29 Skills source/parser/catalog/activation/snapshot tests were included. `scripts/cargo-lock.sh test -p vega_store` passed 134/134, including nine Skills service tests and a red-then-green production v12→v13 migration test that preserves MCP/thread rows and reports zero FK errors. The first Store full run had one old schema-count assertion failure (expected v12/17 tables); after updating that migration-sensitive assertion to v13/24, the full rerun passed. Strict Clippy for both crates and format check passed. The runtime private round-trip proves frozen body/reference/order survival after source change or removal, while the Store service proves a persisted BLOB survives database reopen; **the production controller has not yet connected the two**. No Skills UI, real Agent Loop/provider request, or native app path was exercised, so the rows below remain **NOT RUN**.

| ID | Requirement / risk | Setup and operation | Observable pass criterion | Layer / evidence | Status |
| --- | --- | --- | --- | --- | --- |
| S01 | Project and Vega-owned global discovery | Create one valid skill in each exact root; open a project and refresh Skills | Only immediate `<root>/<name>/SKILL.md` candidates appear; stable sorted order and correct source labels | production discovery + native UI | NOT RUN |
| S02 | UI-authorized external roots | Import an owned external `.agents/skills` root through the UI, then revoke its association | Nothing is scanned before consent; candidate appears after consent and disappears after unlink; original files remain untouched | production controller + native UI | NOT RUN |
| S03 | No ambient home scan | Place decoy skills in unimported `.pi/agent/skills`, `.codex/skills`, and an unrelated home folder | None enter catalog, provider request, or logs | production discovery + provider capture | NOT RUN |
| S04 | Duplicate names and precedence | Same name in project, Vega-owned global, and two imported roots; vary enable/consent | Approved project > Vega global > imported UI order; shadowing visible; unapproved project copy cannot silently replace approved global | resolver + native UI | NOT RUN |
| S05 | Format/size diagnostics | Valid YAML frontmatter (including quoted/block description and optional fields), missing name/description, malformed YAML, duplicate keys, multiple documents, forbidden include/property interpolation, oversize file | Valid candidates load; invalid/oversize/extension cases are disabled with bounded non-secret reason; no partial instruction injection | parser property + UI | NOT RUN |
| S06 | TOCTOU/path fence | Replace a discovered `SKILL.md` with a symlink outside its approved root before activation; also test `..`, absolute paths, nested symlink and hardlink | Activation re-resolves and rejects; zero escaped reads/provider instruction injection; UI shows stale/unsafe state | filesystem race/fault test | NOT RUN |
| S07 | UI consent and hash changes | Start with global/auto off, enable a previewed skill through UI, then alter its file before a new run and refresh | Global and auto work after UI consent; new bytes require renewed UI consent; no unreviewed body is auto-activated; existing in-flight run keeps its original snapshot | controller + native UI | NOT RUN |
| S08 | Catalog-only disclosure | Enable several large skills, submit an unrelated task | Initial provider request contains only bounded name/description catalog, not any unselected body/resource; estimate includes actual catalog bytes | provider request capture + budget | NOT RUN |
| S09 | Model-driven automatic activation | Script provider to call `load_skill` for one matching enabled name, then continue | Host validates frozen catalog and consent, loads only that full body once, records audit, and next provider request sees it before other task tools | production agent loop + mock provider | NOT RUN |
| S10 | No-match/unknown/duplicate | Script no selection, unknown name, repeated name and above-limit activation | No-match proceeds normally; invalid names have stable rejected result and zero file reads; duplicate does not duplicate prompt or accounting | production agent loop + mock provider | NOT RUN |
| S11 | Mixed tool batch | Model proposes `load_skill` beside a mutating/MCP tool in one assistant batch | Skill load is resolved first; other batch calls cannot execute before instructions are available and are deterministically rejected/reissued | production agent loop + audit rows | NOT RUN |
| S12 | Explicit choice and persistence | Select a skill in UI for a thread, send, quit/restart, approve a plan or resume the thread | Selection identity/hash survives restart; approved-plan/retry uses the same frozen body; changed/missing file requires review rather than silent replacement | store/controller reopen + native UI | NOT RUN |
| S13 | Revocation/mis-trigger | Observe an automatically loaded skill, stop the run, disable it in Skills UI, retry | Stop prevents later tools; retry omits disabled skill; UI says past executed actions are not rolled back | controller + native UI | NOT RUN |
| S14 | References/assets progressive load | Activated skill points to text `references/` and binary `assets/`; request only one resource | First read validates root/path/type/size/hash, labels text lower-trust and freezes bytes; other resources stay out of context; binary asset is not misrepresented as text | resource tool + provider capture | NOT RUN |
| S15 | Resource escape/change | Reference path is absolute/parent/symlink/hardlink, or its file changes after the first read | No outside read; repeated read returns the same run snapshot or a typed failure, never silently swapped bytes | resource tool security | NOT RUN |
| S16 | Script/allowed-tools non-authority | Skill says `allowed-tools: Bash(*)`, asks to run a harmless owned script in Ask, ReadOnly, Confirm, Auto and FullAccess | Field grants nothing; script never auto-runs; only the existing bash tool, mode gate, danger check, sandbox and audit can execute it; unavailable global script fails visibly | runtime permission E2E + audit | NOT RUN |
| S17 | Prompt injection | Project skill/resource claims system authority, asks to expose key or bypass tool permission | No new capability, secret/path leakage, or permission bypass; malicious content is labelled lower-trust and trace redacted | adversarial production loop | NOT RUN |
| S18 | #76 budget/compaction | Load a large skill near B=300,000 input/O=128,000 output; trigger compaction during tool loop | Wire estimate counts catalog, active body and MCP schemas exactly once per request; active skill remains available after compaction, while raw history/checkpoints remain unchanged | runtime/conversation E2E | NOT RUN |
| S19 | #73 MCP dependency | Enable a Skill that names a live MCP tool; repeat with server disabled, unauthorized and restarted | Live case invokes namespaced tool under #73 permission/audit; unavailable cases never pretend success or grant skill-derived authority | real local MCP server E2E + fault | NOT RUN |
| S20 | Native end-to-end | In UI import/enable owned global skill, approve project skill, auto-activate by natural-language task, load reference, optionally call safe MCP, restart | Skill provenance/activation/revocation visible; real model answer uses selected instructions, no unselected content; state persists and resumes truthfully | installed Vega + real configured provider + screenshot | NOT RUN |
| S21 | Appearance/accessibility | Skills Settings and active-skill indicator in light/dark and narrow/wide windows; keyboard focus and errors | Existing theme tokens, readable truncation, operable toggles and focus, no false success state | GPUI production test + native pixels | NOT RUN |
| S22 | Failure/limit recovery | Disconnected model, malformed catalog, over-budget skill, missing root, stale UI result, cancelled run | Typed actionable failure, no silent truncation or unauthorized fallback; draft and original transcript preserved | representative fault injection + controller | NOT RUN |

## Before implementation

1. The product scope and matrix were frozen on 2026-09-20; the user approved the exact `serde-saphyr = "=0.0.29"` direct dependency that day. Keep the other provisional defaults in the contract unless the architect changes them; no test row is a substitute for a decision. Do not upgrade the parser or add another crate by implication.
2. Write failing focused tests for S04/S06/S08/S09/S11/S16/S18 before implementation; use real production entry points and owned temporary roots. Keep safety-only seam tests where a real E2E cannot reliably prove the invariant.
3. #74 owns discovery/parser/resource code and Skills UI; #73 owns the shared dynamic tool registry/MCP authority; #76 owns model budget/compaction until its correction lands. One integrator wires shared runtime files serially.

## Required gates and evidence

- `scripts/cargo-lock.sh fmt --all -- --check`, `scripts/cargo-lock.sh clippy --workspace --all-targets -- -D warnings`, `scripts/cargo-lock.sh test --workspace --no-fail-fast`, and `scripts/cargo-lock.sh xtask package`, all with raw exit codes/counts. Shared Cargo target means only one worktree builds/tests at a time.
- Check `cargo tree` for the runtime→UI dependency red line, validate any parser dependency against the exec-guide whitelist/approval, and run `git diff --check`.
- S20/S21 require the installed native production build, real UI-only consent/selection, a real configured provider, a local harmless MCP fixture when #73 is ready, and persistent screenshots with hashes. Mock provider results are not real-network evidence. Preserve first failures and accepted limits in an evidence manifest outside the disposable worktree.
- No closing Issue/Project Done until implementation, full gates, native E2E, master integration, branch/worktree cleanup and Issue write-back are complete.
