# R65 · 滑块填充左端不是胶囊半圆（规格冻结）

> 状态：**SPEC FROZEN**
> 基线：`master @ 5a0c023`
> 发现：2026-09-15，用户实机截图
> 影响：滑块轨道的填充层左端

---

## §1 现象

用户描述：*"它应该像一个胶囊一样左侧应该是半圆形，右侧应该也是半圆形，现在右侧的正确做出的，但是左侧它是半圆形边缘不对，变成一个矩形，所以现在看一下左右根本就不对称。"*

滑块填充的**右端是半圆帽，左端是方角**，左右不对称。

## §2 证据（像素测量，非目测）

在 `master @ 8689f52`（R64）的实机帧 `M2.png`（2× 物理像素）上测量。

**轨道几何**（由代码推导后经像素确认）：

| 项 | 物理 x | 逻辑 x |
|---|---|---|
| 卡片左边框 | 1529 | 764 |
| 卡片右边框 | 2037 | 1018 |
| 轨道左端（= 填充左端） | **1580** | 790 |
| 轨道右端 | ~1986 | ~993 |

**左端（填充起始 x，按行）**：

| y | 蓝色起始 x |
|---|---|
| 1512 | 1580 |
| 1520 | 1580 |
| 1530 | 1580 |
| 1533 | 1580 |
| 1540 | 1580 |
| 1550 | 1580 |

→ **每一行都在同一 x 起始：垂直直边，无弧形。**

**右端（轨道灰色结束 x，按行）**：

| y | 灰色结束 x |
|---|---|
| 1510（上边） | 1976 |
| 1520 | 1980 |
| **1533（中线）** | **1985** |
| 1546 | 1981 |
| 1552（下边） | 1976 |

→ **上下比中间短 9px：半圆帽，弧形正确。**

**结论：左端矩形、右端半圆，左右不对称。缺陷属实。**

## §3 关键判定：这是既有缺陷，不是 R64 引入的

在同一位置的 R64 前帧（`L1.png`，`master @ 3739586`）上采样，填充同样是 y=1510 起整行蓝、左端垂直直边——**与 R64 后逐像素一致**。

R64 之前这个缺陷被那条横穿卡片的 `border_subtle` 线掩盖；R64 修掉那条线后它才显出来。**不要把它当成 R64 回归。**

## §4 根因（GPUI 源码确证）

**`overflow_hidden` 只做矩形裁剪，GPUI 没有圆角裁剪机制。**

`gpui-pre-0.3.4/src/window.rs:2081` 的 `ContentMask` 定义：

```rust
pub struct ContentMask<P: Clone + Debug + Default + PartialEq> {
    /// The bounds
    pub bounds: Bounds<P>,      // ← 只有矩形边界，没有任何圆角字段
}
```

`style.rs:638` 的 `overflow_mask` 据此构造遮罩：它把 `bounds` 收进边框宽度，然后 `Some(ContentMask { bounds })` —— **纯矩形**。

因此轨道的 `.rounded(12).overflow_hidden()` **无法**把子元素裁成胶囊：`rounded` 只作用于**轨道自己的背景**，对子元素毫无影响。

后果：

- **填充左端**与轨道左端重合（都在逻辑 x=790），正落在矩形裁剪边界上 → **从未被裁剪，方角**。
- **右端看似是半圆**，那是**轨道自身背景**的 `rounded`，不是裁剪效果。

**这不是"左端坏了"，而是"从来没有圆角裁剪"**——R64 之前那条横穿卡片的 `border_subtle` 线掩盖了它。

**实现者须知**：不要试图加强 `overflow_hidden`、再加一层裁剪、或调整轨道的 `rounded` 来修复——那条路在 GPUI 里不存在。填充层必须**自己**呈现胶囊形状。

### 实测轮廓（`M2.png`，2× 物理像素，x1560..2010）

蓝色填充逐行的起止 x：

| y | 蓝色 start..end |
|---|---|
| 1510（上边） | 1580..1719 |
| 1520 | 1580..1702 |
| 1533（中线） | 1580..1698 |
| 1546 | 1580..1702 |
| 1557（下边） | 1580..1719 |

**左端：每一行都是 1580 —— 垂直直边（方角，缺陷）。**
**右端：中线 1698、上下 1719 —— 中间凹入，是 knob（白色圆点）覆盖所致，属正确行为。**

### 相关代码

| 行 | 内容 |
|---|---|
| 72 | `THINKING_TRACK_WIDTH: f32 = 203.0` |
| 74 | `THINKING_TRACK_HEIGHT: f32 = 24.0` |
| 77 | `THINKING_KNOB_DIAMETER: f32 = 24.0` |
| 907–913 | 轨道：`w(203) .h(24) .rounded(12) .overflow_hidden() .bg(TRACK_UNFILLED)` |
| 786–793 | 填充：`absolute() .left_0() .top_0() .h(24) .w(fill) .bg(TRACK_FILL_FLAT)` |
| 826 | `dot_center_offset(0, n) = 12.0`（最低档 / Off 的填充宽度） |

## §4b 修复手段（已确定，无需再研究 GPUI）

**做法：给填充层左侧两个角加半径 `THINKING_TRACK_HEIGHT / 2.0`（= 12.0）。**

GPUI 的圆角是**按 quad 尺寸钳制**的，行为完全确定（`gpui-pre-0.3.4/src/geometry.rs:2467`）：

```rust
pub fn clamp_radii_for_quad_size(self, size: Size<T>) -> Corners<T> {
    let max = cmp::min(size.width, size.height) / 2.;
    Corners {
        top_left:     cmp::min(self.top_left, max.clone()),
        top_right:    cmp::min(self.top_right, max.clone()),
        bottom_right: cmp::min(self.bottom_right, max.clone()),
        bottom_left:  cmp::min(self.bottom_left, max),
    }
}
```

即每个角的实际半径 = `min(设定值, min(宽, 高) / 2)`。

**逐档位结果**（`H=24`，7 档，`travel = 203 - 24`）：

| 档位 | fill（逻辑 px） | 钳制后半径 | 左端形状 |
|---|---|---|---|
| 0（最低 / Off） | 12.0 | **6.0** | 被钳制，半径 6 的圆角 |
| 1 | 41.8 | 12.0 | 完整半圆帽 |
| 2 | 71.7 | 12.0 | 完整半圆帽 |
| 3 | 101.5 | 12.0 | 完整半圆帽 |
| 4 | 131.3 | 12.0 | 完整半圆帽 |
| 5 | 161.2 | 12.0 | 完整半圆帽 |
| 6 | 191.0 | 12.0 | 完整半圆帽 |

**关于最低档（fill = 12.0）**：钳制后半径 6.0，画出来是"左侧上下各一个 6px 圆角"，在 12px 宽的方块上**视觉上就是左半圆**（半径 6 = 高度的一半，左右两个角相接）。这与右端的半圆帽在几何上自洽：胶囊的端帽半径就是 `H/2 = 12`，而 12px 宽的填充其左端只有一个 12px 直径的半圆——**两者是同一形状**。

因此**统一用 `THINKING_TRACK_HEIGHT / 2.0` 一个值即可**，不需要按档位分支，钳制逻辑自动处理窄填充。这是本规格推荐的做法。

**不要**研究 quad shader、content mask 对绝对定位后代的裁剪等 GPUI 内部实现——本缺陷的修复不依赖它们，钳制行为已由上面这段源码确定。

### 唯一需要实现者自行确认的点

`rounded_l()` 之类的**左侧两角** API 名称与可用性（本仓库已用的 GPUI 版本可能只暴露 `rounded_tl`/`rounded_bl` 或 `Corners` 构造）。查 `elements/div.rs` 的 `Styled` 实现即可，**不要**为此展开源码调研。

## §5 契约

**R1（必须）** 填充层左端与右端**形状对称**：两端都是半径 = `THINKING_TRACK_HEIGHT / 2` 的半圆帽。

验收判据（可量化）：在选中**最低档**（含 Off，位置 0）时，测量填充左端逐行的起始 x：

- 中线（y 居中）的起始 x 必须**显著小于**上下边缘的起始 x；
- 差值应与右端的对应差值（实测 9 物理像素 @2×）**同量级**；
- 即左端不再是"每一行同一 x"的垂直直边。

**R2（必须）** 不得为了让左端变圆而改变轨道尺寸、knob 直径、档位间距、颜色或 `dot_center_offset` 的既有契约（§4 表格中的常量与 `M5` 未测量项保持原样）。

**R3（必须）** `dot_center_offset` 的语义（两端各内缩半个 knob，使 203px 成为硬边界）不得回退。

**R4（必须）** 修复对**所有档位**生效，不只是最低档：每一档的填充都应是完整的左半圆帽 + 右端（knob 中心处的直边）。

**R5（不得）** 不得删除轨道的 `overflow_hidden` 或 `rounded` 来"统一"成方角——目标是胶囊，不是矩形。

## §6 验收

| # | 证据 | 判据 |
|---|---|---|
| A1 | 实机像素 | 最低档：填充左端逐行起始 x 呈弧形（中线最靠左），与右端对称 |
| A2 | 实机像素 | 中间某档：填充左端同样是半圆帽 |
| A3 | 实机像素 | 轨道整体仍是胶囊：右端半圆帽未被破坏（回归） |
| A4 | 生产测试 | 档位几何不变：各档 knob 中心 x 与改动前一致（`dot_center_offset` 未变） |
| A5 | 生产测试 | 既有滑块测试全部通过（含 R57/R58/R62 的档位与 Off 语义测试） |
| A6 | 门禁 | `scripts/cargo-lock.sh test --workspace` 0 失败 |

**A1/A2/A3 的绘制结果在测试里不可观测**（GPUI 测试平台无 headless renderer），因此由实机像素扫描证明；A4/A5 由生产测试证明。**不要拿 A4 冒充 A1。**

## §7 待实测

| # | 项 |
|---|---|
| M1 | 左端未裁剪的真实原因（`overflow_hidden` 对 `left_0` 的 absolute 子元素的行为） |
| M2 | 修复后是否需要同步调整 `render_track_fill` 的 gradient 分支（最强档） |
