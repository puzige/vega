# Issue #88 — long-history compaction delivery

Issue: https://github.com/puzige/vega/issues/88
Spec: [#76 amendment](vega-issue-76-context-compaction.md#issue-88-amendment--long-tool-history-2026-09-20)

## Freeze and plan

- Baseline: `origin/master` at `79a20c2`; new isolated branch `feat/issue88-long-context-compaction`.
- Observed red case: an automatic compaction attempt at approximately 259K estimated input (configured input 300K) fails `too_large` before the summarizer because a completed history prefix with 122 tool calls serializes beyond 128 KiB. Original history and configuration are not modified for this task.
- Sequence: (1) add red reproduction for production conversation-to-runtime path, recording the expected failure; (2) implement bounded staged summarization and accurate error copy without schema/dependency change; (3) add L02–L04 fault/persistence checks; (4) run full workspace gates and test build; (5) native UI + real provider L05, inspect screenshot and preserve evidence outside the worktree; (6) review, squash merge, cleanup and close card.
- Build/test lock: `scripts/cargo-lock.sh`. Full gates: `cargo fmt --all -- --check`, `scripts/cargo-lock.sh clippy --workspace --all-targets -- -D warnings`, `scripts/cargo-lock.sh test --workspace`, plus packaged candidate and native E2E. Do not run concurrent Cargo against the shared target.
- Rollback: revert the #88 squash commit. Checkpoints remain append-only and raw transcript/tool rows remain authoritative; no migration expected.

## Acceptance matrix

The L01–L05 definitions and expected observations are frozen in the linked spec. The red production-path test first failed with `SourceTooLarge` before any summary request (exit 101, 0 passed/1 failed; `l01-red.log`). The focused `issue88_` conversation tests now pass (7 passed/0 failed): L01 long completed-call history continues in the same run; L02 a single oversized tool input/result is fully and chronologically staged, including an explicit empty-result marker; L03 later-stage failure, cancellation and source mutation do not install a partial checkpoint while preserving observed usage; L04 a restart projects the one durable checkpoint and does not replay a prior tool. The additional cases model bounded output exhaustion and a dense later stage. They do not prove real provider behavior. The UI copy test distinguishing `too_large` and `over_limit` passed (1/1). L05 failed on intermediate native attempts; candidate `3851ebb` subsequently succeeded and restored after restart. The A1/C1/C2 follow-up candidate passed the real-provider fixture and final installed native continuation; see final acceptance below.

## Evidence

Implementation: `8617f6d` on the isolated branch; no dependency or schema change. The effective whole-source 128 KiB dead end is replaced by bounded summary requests, with a 64 MiB planning ceiling and at most 512 stages; the raw transcript remains authoritative and any exceeded safety cap fails explicitly rather than silently dropping data. The agent can resume in the same run after a successful final checkpoint.

Local gates (2026-09-20): `cargo fmt --all -- --check` exit 0; `./scripts/cargo-lock.sh clippy --workspace --all-targets -- -D warnings` exit 0; `./scripts/cargo-lock.sh test --workspace` exit 0, 35 test-result groups, 1,632 passed / 0 failed / 9 ignored. The focused red/green logs, these gate logs and native evidence are retained in the owner-only `vega-evidence/issue-88-2026-09-20.dCZjA0` directory outside disposable worktrees. The red log SHA-256 is `ac4a897feabbcd6ec7a7d66f63181d58ffea7e7c2832e8cc563a007ef646de02`; the focused green log SHA-256 is `f248007dd222f80336b1e4c400930582ad925d73a81c68b1c5aa6e50c8281314`. Packaging, installed native E2E, PR/integration and final screenshot manifest are pending. Private transcript content and paths are omitted here.

## Native acceptance follow-up

The reported sidebar loading state was not reproduced on resumed inspection or subsequent candidate restarts; the original session opened without database or configuration edits. Native input automation could not reliably submit the GPUI Composer, so the owner sent harmless continuation requests through the UI.

The first staged candidate contacted the real configured provider but failed `invalid_summary` with reported output exactly at its 1,024-token cap. A 4,096-token candidate completed its first stage (1,852 output tokens) and failed its second (4,096). The owner then explicitly requested an 8,192-token cap. With a 32 KiB stage request target, that candidate completed the first stage (3,099 output tokens) and failed the second (8,192). Exact provider stop reasons were not persisted; truncation is an inference from cap-sized output and the typed failure, not a captured wire assertion. All these failures kept zero checkpoints and preserved original records.

The intermediate `3851ebb` candidate retained the owner-requested 8,192-token output cap, bounded by the configured reserve, and the 32 KiB per-stage request target. The 128 KiB hard request ceiling, 32 KiB collected-summary text limit, 60-second stage timeout, 64 MiB source plan and 512-stage ceiling remain bounded. There is no automatic retry of a failed stage. The summary prompt now selects task-relevant goals, explicit constraints, decisions, changes, key results and unresolved work; it no longer simultaneously demands a short summary and every incidental unique fact in tool output. Source coverage and raw history are unchanged.

Both the pre-prompt and prompt-only revisions passed full gates with 1,634 passed / 0 failed / 9 ignored (`8192-final-*` and `prompt-final-*` logs), formatting, strict Clippy and packaging. The prompt contract test passed (1/1), as did focused compaction tests (7/7). String assertions validate the prompt contract only; they do not establish provider reliability.

Intermediate packaged code: `3851ebb`; installed executable SHA-256: `a63932df98a0753801e756997e0458aec84d5db39012c66151c70e4f9d2f0d14`. On this same candidate, one native attempt failed after a cap-sized second-stage response; the next owner-initiated attempt completed ten summary stages, installed one checkpoint, resumed the primary request and replied with the requested harmless acknowledgement. Native screenshots show both the earlier failures and later success. After restart, the original thread reopened with the completed reply and compaction status intact. A read-only comparison verified every field of the original 10 messages and 122 tool records unchanged. The raw-history check, stage usage and screenshots remain local. This single success does not establish that the earlier intermittent invalid-summary failure is eliminated. Issue closure, Project Done, publication, merge and task cleanup remain pending.


## Reference-guided follow-up (in progress)

The owner supplied the remediation guide at reference commit `432f119` and asked to apply it. The #88 spec now freezes the first batch A1/C1/C2: estimator v2 with separate text/typed-JSON estimates and conservative request factor; nine-section rolling summaries without the 400-token instruction, retaining the requested 8192 output cap; and a shared, untrusted-history continuation wrapper for live/reloaded checkpoints. The richer summary must fit alongside a bounded new-source segment, with the existing 128 KiB complete-request ceiling and configured token budget enforced.

Architect decisions: old estimator metadata does not invalidate existing checkpoints; re-estimate their projection with v2. Omit nonexistent transcript paths/tools. Preserve system authority, complete source coverage, source fingerprint/CAS, atomic checkpoint installation, tool pairing and restart protection. Later guide batches are outside this incremental fix. In particular, source dropping and system-message authority changes are not adopted.

At specification freeze, R88-A1/C1/C1b/C2/E1/E2 were NOT RUN; subsequent results are recorded below. Delegated implementation must demonstrate red tests before changes and rerun the affected production regressions. Main agent owns review and final native acceptance. Since v2 may put the original session below the trigger, ordinary continuation alone cannot prove the final automatic path; exercise an owned fixture with a real provider in addition to the original-session UI/restart check. Never modify the user's database/settings to force a pass.


### Follow-up implementation and real-provider evidence

The reviewed code applies A1/C1/C2 and corrects an existing pre-push invocation defect (`cargo cargo test`); all Clippy/test/build gates now use the same locked dispatcher. An owned argv recorder verified wrapper-present/fallback routing and failure propagation (8/8). This is not a replacement for the real pre-push gate.

The estimator, summary and continuation-wrapper regressions were demonstrated red before their fixes. Updated v1 fixtures preserve the original behavioral assertions. Focused passes include runtime library 180/0 (4 ignored), estimator/context 10/0, existing conversation-compaction cases 15/0 and #88 cases 11/0. Full final gates remain pending.

Real provider follow-up passed through the production automatic conversation path using an isolated synthetic fixture: 153,840 bytes of completed tool output; estimated input 63,791; input budget 70,170; trigger 56,136; target 42,102. Six summary requests ended normally, with 1,077–2,149 output tokens each and full-request text below 128 KiB. The primary continuation and a second real reply after reopening also ended normally: eight calls total, zero tool proposals, unchanged 120 fixture tool rows, and preserved early read-only constraint, project identifier and user-corrected format. The early facts appeared only at the start of the fixture; the final question did not repeat their answers. The run completed in 53.28 seconds (1 passed, exit 0). Manual inspection of the final synthetic summary confirmed all nine sections, the earlier parsing error and explicit user correction. The temporary observer delegated unchanged requests/responses to the production provider and credential guard; it does not ship with the application.

Residual: the configured gateway emitted nonzero thinking content even though summary requests carried the declared disabled reasoning choice. Thus a short visible summary does not imply negligible generated output. Actual usage and stop reasons are retained locally; this observation does not prove the exact reason for earlier native failures, whose stop reasons were not captured. Provider truncation/timeout remains a visible failure with no partial checkpoint. No model configuration or credentials were changed.

The owned fixture proves the final automatic code path against a real provider; installed native continuation/restart remains separately pending. It is not evidence that new user history was generated through the native Composer or that every provider response is guaranteed to fit.


R88-E1 short-fixture comparison also passed: the exact prior four-section prompt and new nine-section prompt each received the same synthetic history through the same real provider, with the same 8192 cap. Both ended normally and retained all four reviewed facts (project identifier, read-only constraint, CSV parsing error, TSV correction), with zero tool calls. Old/new visible text was 607/3,803 bytes; reported output was 857/1,004 tokens. The new result preserves explicit request chronology and the nine sections. This one comparison does not establish a statistical quality or cost advantage. Private metadata and synthetic summaries were manually inspected. The temporary diagnostic source was archived locally and removed from the delivery tree; the reviewed permanent code diff hash remained unchanged.


Final-gate failures are retained: strict Clippy found one unnecessary clone in a new test (fixed with `slice::from_ref`, no production effect). The first workspace run then found a real regression in the unchanged native first-image-send test: applying the new factor to the already-conservative image contribution moved the existing 229,411-token image above 300K. The architect corrected the A1 specification to factor non-image accounting only and retain image contributions unchanged. The original native fixture/assertions must pass unchanged; this is a production correction, not a test exemption. Final gates are pending rerun.


### Final candidate gates

Code commit `0ad6787` passed formatting and strict workspace/all-target Clippy (`remediation-release2-fmt.log`, `remediation-release2-clippy.log`). The image-accounting regression was demonstrated red, then corrected; all 10 context tests and the original unchanged native first-image-send test passed. The next full run reported `ProcessControlFailed` instead of `OutputTooLarge` in an unchanged Git process-fault test. Its original failure is preserved (`remediation-release2-workspace-test.log`); the exact isolated test passed, and a subsequent unmodified complete workspace run passed **1,640 / 0 failed / 9 ignored**, across 35 result groups (`remediation-release3-workspace-test.log`, exit 0, 111.08 s). The intermittent process-test result was not hidden, relaxed or removed.

The real-provider text fixture above used the reviewed compaction implementation; the only later production change retained the existing image contribution outside the new non-image factor. No summary request, prompt, checkpoint or continuation code changed after that real-provider run. Packaging passed (exit 0, 48.92 s). The installed binary matches the packaged binary: SHA-256 `3ea5e675efb4ca06502e2b16a81cdec3bb035805fef8f1c4101066eb652a94f0`; strict deep codesign verification passed. The final candidate reopened the original session and its existing checkpoint without a loading stall. The owner-operated harmless continuation passed; final acceptance is recorded below.


### Final installed native acceptance

The owner confirmed the final repair works. A read-only inspection found the new original-session assistant reply at sequence 22 completed with the exact requested acknowledgement, following the owner-submitted harmless request at sequence 21. The installed executable still matches code `0ad6787` and SHA-256 `3ea5e675efb4ca06502e2b16a81cdec3bb035805fef8f1c4101066eb652a94f0`. The final build had already been restarted and reopened this original conversation before the submission: checkpoint 4 (`source_version=122`, `covered_through_seq=18`, legacy estimator v1) restored successfully and remained unchanged after the new reply. All original 10 messages and 122 tool rows compare equal, field for field, with the preserved pre-test database. No database/configuration edits were made.

R88-A1/C1/C1b/C2/E1/E2 are accepted with separate evidence: focused red/green regressions; owned real-provider automatic six-stage compaction, one checkpoint and subsequent real replies after reopen; and the final installed application's owner-operated continuation using the original session and restored checkpoint. The final original-session continuation reused its existing checkpoint and did not claim a new automatic compaction. The final-build restart screenshot and read-only metadata are private local evidence. An additional post-reply restart was deliberately not performed while another user task was active; the already-observed final-build checkpoint restoration is the restart evidence.

Main-agent review checked bounded complete source coverage, call/result provenance, no partial checkpoint on failure/cancel/source mutation, usage across stages, stop/envelope validation, live/reload wrapper parity, v1 compatibility, unchanged image accounting, dependency/schema invariants, and fail-closed pre-push routing. No unresolved implementation findings remain. Publication/integration and exact #88 cleanup are the remaining delivery steps.
