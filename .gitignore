target/
.DS_Store
*.log
# xtask bench results are generated artifacts, not source (T04 / E4)
bench/results/
# agent working data (sync reports etc.)
.workbuddy/
# xtask package artifacts (dist app bundle + zip never enter the repo; the
# .icns is reproducible from the committed assets/logo SVG)
dist/
